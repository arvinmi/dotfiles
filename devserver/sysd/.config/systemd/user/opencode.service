[Unit]
Description=OpenCode Server

[Service]
ExecStart=/bin/sh -c 'exec %h/.nvm/versions/node/v22.21.0/bin/opencode serve --hostname $(tailscale ip -4) --port 4096'
Restart=on-failure

[Install]
WantedBy=default.target
